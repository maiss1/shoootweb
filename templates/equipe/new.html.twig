{% extends 'baseback.html.twig' %}

{% block Container %}
    
    <section class="content">
    <div class="container text-center">
    <h1>Create new Equipe</h1>
        
    
        {{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
        {{ form_widget(form) }}
    
        
        <div class="card-body"><a class="btn btn-success" style="width: 50%" href="{{ path('app_equipe_index') }}">Afficher Publication</a></div>

       {{ form_end(form) }}
    
       
    
    </div>
    </section>
{% endblock %}
