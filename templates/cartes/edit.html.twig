{% extends 'basefront.html.twig' %}


{% block Container %}
    <h1>Edit Cartes</h1>

    {{ include('cartes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cartes_index') }}">back to list</a>

    {{ include('cartes/_delete_form.html.twig') }}
{% endblock %}
