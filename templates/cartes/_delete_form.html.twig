<form method="post" action="{{ path('app_cartes_delete', {'id': carte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte.id) }}">
    <button class="btn"><button type="button" class="btn btn-danger mb-2">delete</button></a></button>
</form>
