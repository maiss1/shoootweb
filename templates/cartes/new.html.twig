{% extends 'baseback.html.twig' %}


{% block Container %}
    <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div  class="form-control" style="height: 45rem; margin-top: 5rem;">
                        <form id="quickForm">
                            <div class="card-body">
    <h1>Create new Cartes</h1>

    {{ include('cartes/_form.html.twig') }}

    <a href="{{ path('app_cartes_index') }}"><button type="button" class="btn btn-primary mb-2">back</button></a></a>
        </div>
                        </form>
                    </div>
                </div>
            </div>

        </section>
{% endblock %}
