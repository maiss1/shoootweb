{% extends 'basefront.html.twig' %}

{% block Container %}

    <h1>Edit Matchh</h1>

    {{ include('matchh/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_matchh_index') }}">back to list</a>

    {{ include('matchh/_delete_form.html.twig') }}
{% endblock %}
