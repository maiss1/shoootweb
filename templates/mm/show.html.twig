<!DOCTYPE html>

<title>Matchs</title>

{% block body %}
    <h1>Matchs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ match.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mm_index') }}">back to list</a>

    <a href="{{ path('app_mm_edit', {'id': match.id}) }}">edit</a>

    {{ include('mm/_delete_form.html.twig') }}
{% endblock %}
