<!DOCTYPE html>

<title>Edit Matchs</title>

{% block body %}
    <h1>Edit Matchs</h1>

    {{ include('mm/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_mm_index') }}">back to list</a>

    {{ include('mm/_delete_form.html.twig') }}
{% endblock %}
