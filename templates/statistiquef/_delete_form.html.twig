<form method="post" action="{{ path('app_statistiquef_delete', {'idd': statistique.idd}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statistique.idd) }}">
    <button class="btn">Delete</button>
</form>
